"""
PPT configuration for the company section (independent engine build).

This is an example configuration file. Copy this to config_pptx_company.py and set your API key.
"""
import os
from pathlib import Path

BASE_DIR = Path(__file__).resolve().parent

# API settings - USE ENVIRONMENT VARIABLE
ANTHROPIC_API_KEY = os.getenv("ANTHROPIC_API_KEY", "")
if not ANTHROPIC_API_KEY:
    raise ValueError(
        "ANTHROPIC_API_KEY is not set. "
        "Please set it as an environment variable or in a .env file."
    )

CLAUDE_MODEL = "claude-3-5-sonnet-20241022"
CLAUDE_MODEL_FALLBACKS = [
    "claude-3-sonnet-20240229",
    "claude-3-haiku-20240307",
    "claude-3-opus-20240229",
]
LLM_WORD_COUNT = 280

# Paths
DOWNLOADS_PATH = os.path.join(os.path.expanduser("~"), "Downloads")
SEED_TEMPLATE_PATH = str(BASE_DIR / "assets2" / "ESG_Templet2.pptx")
OUTPUT_PATH = str(BASE_DIR / "output")
ASSETS2_PATH = str(BASE_DIR / "assets2")
ASSETS3_COMPANY_PATH = str(BASE_DIR / "assets3" / "company")

# ... rest of configuration ...

